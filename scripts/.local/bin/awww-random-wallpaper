#!/usr/bin/env bash

wallpapers_dir="$HOME/Pictures/Wallpapers"
transition="${1:-random}"

random_dir=$(find "$wallpapers_dir" -mindepth 1 -maxdepth 1 -type d ! -name "current" | shuf -n 1)

dirname=$(basename "$random_dir")

"$HOME/.local/bin/awww-set-wallpaper" "$dirname" "$transition"
